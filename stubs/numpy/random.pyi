# This stub is incomplete. It only contains the functions we use.

from . import ndarray
from typing import Optional, Union, Tuple


def ranf(size: Optional[Union[int, Tuple[int, ...]]]) -> ndarray[float]: ...
